# P0 — live checklist

- [x] Backend runs (`uvicorn app.main:app` — verified locally)
- [x] Sim vehicle state returns (`/api/vehicle`; static profile until Smartcar creds added; Smartcar simulator path implemented)
- [x] NL input accepted (UI input → `POST /api/chat` → job)
- [ ] Gemini genuinely tool-calls (loop implemented + logged per-call; **needs `GEMINI_API_KEY` to verify live**)
- [ ] Route retrieved (live OSRM code + auto-cached fixture + labeled straight-line fallback; **live call must run on a network that can reach OSRM — blocked in the build sandbox, verified working today from user's network**)
- [ ] Chargers retrieved (AFDC/NTAD query + auto-cached fixture; same network note as above)
- [x] Optimizer feasible plan (11 unit tests: ATL→Nashville 248.7 mi @ 42% → 1–2 stops, feasible)
- [ ] Gemini explains (needs key)
- [x] UI shows itinerary (three states: connected / planning with real step ticks / result timeline + assumptions)
- [ ] Repeatable ×2 (run on user machine with key: same prompt twice)
- [ ] Deployed-or-localhost-stable

## To go green on the remaining boxes (user machine, ~10 min)
1. `pip install -r requirements.txt`
2. `cp .env.example .env`, paste `GEMINI_API_KEY` (+ Smartcar creds when ready)
3. `uvicorn app.main:app --port 8000` → open http://localhost:8000
4. Run the demo prompt once (this auto-caches OSRM + AFDC fixtures for demo resilience), then twice more for the ×2 check.
5. Optional Smartcar: click "Connect vehicle via Smartcar" (redirect URI must match dashboard). Falls back to labeled static profile automatically.

## Kill rules honored
- No Gemini key → labeled deterministic direct mode (never simulated tool calls)
- Smartcar failing at 7:00 → static profile labeled "demo vehicle profile (static)" (automatic)
- Render Workflows: NOT attempted (P0 gate not green before 7:00 in-sandbox)
